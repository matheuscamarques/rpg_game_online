text = "0";       // O valor que será mostrado
color = c_white;  // A cor (Branco p/ player, Vermelho p/ inimigo)

vsp = -3;         // Velocidade vertical inicial (sobe rápido)
gravity_force = 0.1; // Gravidade leve (para desacelerar a subida)

alpha = 1.5;      // Começa maior que 1 para ficar visível um tempo antes de começar a sumir
scale = 1.5;      // Começa grande (Impacto)
scale_target = 1.0; // Diminui para o tamanho normal